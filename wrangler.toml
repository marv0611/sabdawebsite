name = "sabda-checkout-proxy"
main = "cloudflare-worker-checkout-proxy.js"
compatibility_date = "2024-01-01"
account_id = "ac63756828d402343fc988ec9f161f56"

# KV binding intentionally NOT declared here — managed via Cloudflare dashboard.
# Dashboard: Workers & Pages → sabda-checkout-proxy → Settings → Bindings
# Binding name: ATTRIBUTION_KV
# Wrangler deploy preserves dashboard-set bindings as long as they aren't
# overridden here.
